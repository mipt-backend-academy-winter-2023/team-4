version: "3.8"

services:
  auth:
    build:
      context: ./
      dockerfile: Dockerfile-auth
    ports:
      - "8081:8080"
  routing:
    build:
      context: ./
      dockerfile: Dockerfile-routing
    ports:
      - "8082:9090"